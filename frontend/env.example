# 複製這個檔案為 .env.local，填入實際的值
# .env.local 已在 .gitignore 中，不會被 commit

# 後端 API URL
# 開發時：http://localhost:5000
# 部署後：填入 Render 給你的 URL，例如 https://course-review-backend.onrender.com
VITE_API_BASE_URL=http://localhost:5000
